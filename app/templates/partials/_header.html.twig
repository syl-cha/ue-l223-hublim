<nav class="d-flex justify-content-around align-items-center py-3">
    <div style="display: flex; flex-direction: column; align-items: flex-start; gap: 10px;">
        <div>
            <img src="{{ asset('svg/logo_hublim_name.svg') }}" alt="Logo HubLim" style="width: 120px;" />
        </div>
        {% if app.user %}
            <div>
                Bonjour <strong>{{ app.user.firstName }} {{app.user.lastName|upper}}</strong>
            </div>
        {% endif %}
    </div>

    <ul class="d-flex list-unstyled mb-0">
        <li class="mx-5"><a href="#" class="text-decoration-none">Accueil</a></li>
        <li class="mx-5"><a href="#a-propos" class="text-decoration-none">A propos</a></li>
        <li class="mx-5"><a href="#contact" class="text-decoration-none">Contact</a></li>
    </ul>

    <div>
        {# Utilisateur connecté → Bouton Déconnexion #}
        {% if app.user %}
            <a href="{{path('app_logout')}}" class="btn btn-orange">Déconnexion</a>
        {# Utilisateur non connecté → Bouton Connexion/inscription #}
        {% else %}
            <a href="{{path('app_login')}}" class="btn btn-orange">Accéder aux cartes</a>
        {% endif %}
    </div>
</nav>